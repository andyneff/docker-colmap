version: "2.4"

services:
  colmap:
    build:
      context: .
      args:
        DSM_CUDA_ARCHS: ${DSM_CUDA_ARCHS-Auto}
    image: colmap
    runtime: nvidia
    volumes:
      - type: bind
        target: /data
        source: ./data